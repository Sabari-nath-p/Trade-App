import 'package:flutter/material.dart';

class CreateAccountBtCard extends StatelessWidget {
  const CreateAccountBtCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
