import 'package:flutter/material.dart';

class MyAccountWebView extends StatelessWidget {
  const MyAccountWebView({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(children: [
        
      ],
    );
  }
}
