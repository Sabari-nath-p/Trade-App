import 'package:flutter/material.dart';

class TradeContextCard extends StatelessWidget {
  const TradeContextCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
